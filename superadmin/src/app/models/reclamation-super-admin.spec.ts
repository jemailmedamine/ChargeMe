import { ReclamationSuperAdmin } from './reclamation-super-admin';

describe('ReclamationSuperAdmin', () => {
  it('should create an instance', () => {
    expect(new ReclamationSuperAdmin()).toBeTruthy();
  });
});
